---
topic: Personal concert attendance log
updated: 2026-08-18T11:41
---

- (direction) Rebuild LiveMemory from zero on the factory Nuxt app; keep the concert-log idea from LiveMemorySpec, do not copy that codebase.
- (decision) CAP-1: music fans can create an account and CRUD their own concert records (artist, date, place, optional notes/stage).
- (decision) CAP-2: users can group concerts into events (single-night lineups or multi-day festivals) and reassign standalone concerts without duplicating them.
- (decision) CAP-3: event date range, inherited place, and optional stages constrain concert entry; invalid saves are blocked with a specific message.
- (decision) CAP-4: sharing is opt-in and read-only at a public URL; private by default; visitors cannot edit even if signed in as someone else.
- (constraint) Private by default; no collaborative editing; individual accounts only.
- (constraint) Factory stack: Nuxt 4, Nuxt UI, Pinia stores for remote data, SQL migrations + RLS, Playwright against local Supabase only, no PWA, no Prisma.
- (constraint) Duplicate-looking concerts (same artist, date, event, stage) are allowed after a warning; deleting an event with concerts requires keep-or-delete choice.
- (assumption) Place is free-text practical location (venue, city, festival site, or combination).
- (assumption) Event place inheritance is the default; per-event override can allow per-concert places.
- (question) Should v1 include import from existing LiveMemory data, or only plan for it later (FR-017)?
- (note) Public URL is https://livememory.pierre-reynaud.fr; GitHub repo remains livememory-app.
- (event) Self-validate pass 1 (coherence): Spec Law 1-6 and 8 hold; four CAPs with intent+success; non-goals present; success signal testable.
- (event) Self-validate pass 2 (preservation): LiveMemorySpec FRs and edge cases landed in SPEC.md or entities.md; FR-017 left as open question; DESIGN.md visual tokens not absorbed (UX later).
- (event) Spec created at _bmad-output/specs/spec-livememory/
- (decision) User: v1 does not import existing LiveMemory data; import is planned for later (FR-017 deferred).
- (event) Open question on FR-017 import resolved: defer. Open Questions section now empty.
- (event) Self-validate after update: coherence still holds; preservation keeps FR-017 as explicit non-goal, not a v1 capability.
- (decision) Every Concert belongs to exactly one Event. No standalone Concerts. A single-performer show is an Event with one Concert.
- (decision) Event creation may be explicit or transparent: adding a first Concert to a new single-night show creates its Event; later Concerts are added to the same Event. Events may exist before Concerts and prefill Concert fields.
- (decision) Events are editable. An incompatible update is blocked and names affected Concerts until resolved. Deleting a non-empty Event requires explicit confirmation and deletes its Concerts; keeping them standalone is removed.
- (decision) Concert Attendance is optional: going or attended; unset means Bill-only. going becomes attended after optional time or Europe/Paris end-of-day. Single-night attend-all applies to existing and later-added Concerts; festivals have no attend-all.
- (decision) Shared List is an opt-in public profile at a URL derived from the unique username. It shows only going or attended Concerts grouped by Event; Events with no visible Concerts are hidden; notes and Bill-only Concerts stay private.
- (capability) CAP-5: owner plans and records Attendance (going, automatic attended, or unset Bill-only), including single-night attend-all.
- (capability) CAP-6: authenticated MCP can read and manipulate Events, Concerts, and Attendance under the same rules after UI CRUD exists; screenshot interpretation remains outside LiveMemory.
- (event) PRD reconciliation update started from prd-livememory-2026-08-17; prior standalone and attended-only decisions are superseded by later entries.
- (assumption) Changing the unique username changes the public profile URL and invalidates the previous URL; confirm before PRD finalization.
- (event) Self-validate pass 1 (coherence): Spec Law 1-6 and 8 hold; CAP-1..CAP-6 each have intent and success; mandatory Event, Attendance, sharing, and MCP semantics are consistent.
- (event) Self-validate pass 2 (preservation): load-bearing PRD changes landed in SPEC.md or entities.md; obsolete standalone and did_not_attend decisions remain historical and are superseded by later entries.
- (decision) Username rename and public-profile URL migration behavior are deferred; they do not block v1. v1 requires only a unique username and username-derived profile URL.
- (decision) v1: one shared Event record. Only the Event owner can edit or delete the Event and Bill. Other signed-in Users find it via an unguessable Event link (no search). Attendance and notes are per User. Public username profile remains a separate opt-in surface.
- (capability) CAP-7: signed-in Users can open an Event link, view the shared Bill, and set their own Attendance without editing the Bill.
- (event) Self-validate after CAP-7: coherence holds; Event owner vs joiner, Event link, per-user Attendance/notes, public profile kept. Collaborative Bill edit and Event search remain non-goals.
- (event) Update from reconciled PRD 2026-08-17: Home+Concerts IA, Shared List tap-to-join, owner-only notes, one-shot attend-all, username at register. Preserve CAP-1..CAP-7.
- (decision) Username chosen at registration (a-z0-9_-, case-insensitive unique, immutable in v1). After sign-in, User lands on Home. Concerts is the full owned+joined log. Home shows next 1-3 upcoming plus three souvenir stats; more stats later.
- (override) Supersedes Shared List as non-join surface. Tapping a visible Event on Shared List opens it (FR-18): sign-in if needed, join, full Bill, own Attendance. Profile page stays read-only. Joiner can leave.
- (override) Supersedes attend-all later-added flag: one-shot on Concerts currently on a single_night Bill. Owner Attendance default only on transparent one-Concert create. Notes are Event-owner-only; joiners have Attendance only.
- (decision) Transparent Event default name is Concerts on DD/MM/YYYY at Place. Joiner can leave (Attendance deleted); no rotate/kick/roster. Empty Event deletable; last Concert delete/move leaves Event empty. Attendance: past attended|unset, future going|unset, no attended to going.
- (constraint) Shipped UI copy is English; going label is Going. MCP authenticates as the acting User. Event start/end dates inclusive. Concert move does not auto-join source joiners to the target.
- (event) Self-validate pass 1 (coherence): Spec Law 1-6 and 8 hold. CAP-1..CAP-7 kept; intent+success updated. Non-goals include joiner notes, extra Home stats, rotate/kick.
- (event) Self-validate pass 2 (preservation): PRD load-bearing claims landed in SPEC.md or entities.md. UX IA/visual adopted as companions EXPERIENCE.md and DESIGN.md. Wrapper journeys and SM copy omitted.
- (change) Dropped machine-local LiveMemorySpec path from `sources:`. PRD + addendum in this repo are the traceability sources; the original speckit file is not vendored.
- (decision by user) Concert create identity (AD-10): Event owner's journal, not worldwide. Artist match case-insensitive. Same owner+artist+date+clock time: attach to existing, do not insert, do not reparent. Same identity at a different effective Place: refuse. Same owner+artist+date with time missing on one or both: ask attach (may then set time) or create a second Concert; UI and MCP offer the same choice. Same artist+date different times: allowed. Overrides warn-then-allow same artist+date+Event+Stage.
- (event) Self-validate pass 1 (coherence): Spec Law 1-6 and 8 hold. CAP-1..CAP-7 IDs preserved; CAP-1 and CAP-6 success now name Concert-identity outcomes. Warn-then-allow constraint replaced by AD-10 identity rule. Non-goals unchanged.
- (event) Self-validate pass 2 (preservation): AD-10 (1)-(4), owner-journal scope, case-insensitive artist, no reparent on attach, UI+MCP same missing-time choice, and effective Place landed in SPEC.md + entities.md. Domain return codes stay in architecture (HOW). PRD FR-13/FR-17 and UX EXPERIENCE updated as sources/companion so stories do not keep warn-then-allow.
